In this example, we get all eshop categories in blade template;

    eshop()->category()->get()

In this example, we get all eshop products in blade template;

    eshop()->product()->get()

In this example, we get all eshop products grouped by category in blade template;

    @f
